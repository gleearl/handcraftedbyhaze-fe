# The shop is served from handcraftedbyhaze.com and the API from its own
# subdomain, so this is set rather than blank. Same site, different origin:
# the session cookie is scoped to .handcraftedbyhaze.com and rides along.
VITE_API_URL=https://api.handcraftedbyhaze.com
